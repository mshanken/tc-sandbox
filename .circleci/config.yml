# Javascript Node CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-javascript/ for more details
#
version: 2
jobs:
  build:
    machine:
      docker_layer_caching: false
    steps:
      - checkout
		  - run: docker-compose build -f ./docker-compose.prod.yml
workflows:
  build:
    jobs:
      - build
    version: 2
